var config = {};

config.listRegistry = ["https://localhost:3000", "https://localhost:3001", "https://registry.npmjs.org"];

module.exports = config;

//let url = self.baseURL + "/" + name + "/-/" + name + "-" + versionTarget + ".tgz";